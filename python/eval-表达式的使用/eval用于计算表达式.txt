eval（expr)

这个expr是个字符串的表达式：
比如expr = "a * b + c"

如果发现有a,b,c这些变量

会自动解析出变量（并解析出其类型）

很有用：

比如，我有个表达式：
expr = "CZ3-FC7007/CZ3-FC7002*100"

另外，在我的df中，上面的expr包含两个变量名

直接将上面的2个变量用str.replace的方法全部替换成 "df[\"" + 变量名 + "\"]"

得到新的expr,看起来是这样的：
df["CZ3-FC7007"]/df["CZ3-FC7002"]*100