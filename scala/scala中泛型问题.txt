scala使用泛型时，需要在类中定义；

即只有接口或类  这种可以使用泛型，即定义一个抽象类。


在接口中可以直接定义泛型：

例如：

1个接口：
trait Imodel[T]{ }

其实现类：

class SomeModel[T] extends Imodel[T]{ }


但如果没有定义接口的情形下，我们不能直接就用泛型，

需要在程序前面加一个defined class:
class T

这样就在该类中，定义了某个T这个类， 就是一个标记，表示这是一个类，不必定义其具体内容。

