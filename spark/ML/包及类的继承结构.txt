以ML模块为例：

ML下的：1级子目录基本是各种具体模型所在的包名（模型也有分类，如分类聚类等）

	而1级子文件，基本是超类（顶级父类、顶级接口，如Param,Transformer,Estimator,Predictor），或者最外层逻辑的接口（如pipeline）

在1级子目录下（各模型所在大分类）下面，则不同，基本只有子文件
子文件里面，一般都有一个抽象类或接口，如分类的Classifier，或者回归的Regressor

以后也可以按此法去写。


