1.代码重构：将源码合在一起，写个主函数之类的，理出一个项目目录（基本构建，类spark目录sbin，bin,conf，example，data，lib等等，比如我的，就是将模型类放到models里）————总之，这个就仿照spark的源码来。

2.小的功能集合起来。

	写成函数形式，def， 自己写到专门的utils类里

	小功能的分类：（数据处理、特征转换、特征筛选、特征提取，想个结构层次，将这些utils函数，分类放到一起）



